<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:mvc="http://www.springframework.org/schema/mvc" 
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans 
    http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
     http://www.springframework.org/schema/context 
        http://www.springframework.org/schema/context/spring-context-3.0.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd">

	<!-- ※ 사용자 정의 Controller 객체 등록 및 URL 매핑 주소 -->	
	<!--    - 『name=""』 속성에 URL 매핑 주소를 등록한다. -->
	<!--       이 과정에서 Front Controller 가 갖고 있는 확장자의 형태로 구성한다. -->
	<!--    - 『class=""』 속성은 Controller 객체의 클래스 경로 및 이름을 등록한다. -->
	
	<context:component-scan base-package="com.playstamp.home.mybatis"/>
	<context:component-scan base-package="com.playstamp.mseat.mybatis"/>
    <!-- → 『base-package』 속성에 Controller 객체가 포함된 패키지 경로를 지정한다. -->
    <!-- 이러한 패키지 경로를 알려줄테니까 이안에 설계도를 읽어서
    필요에 따라 그 설계도에 따라 만들어서 쓰면 돼 -->
    
    <context:annotation-config />

    <mvc:annotation-driven />
    
    <bean id="localDataSource" class="org.springframework.jdbc.datasource.SimpleDriverDataSource">
    	<property name="driverClass" value="oracle.jdbc.driver.OracleDriver"></property>
    	<property name="url" value="jdbc:oracle:thin:@211.238.142.163:1521:xe"></property>
    	<property name="username" value="playstamp"></property>
    	<property name="password" value="java006$"></property>
    </bean>
    
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
    	<property name="dataSource" ref="localDataSource"></property>
    	<property name="mapperLocations" value="classpath:com/playstamp/mybatis/mapper/*.xml"></property>
    </bean>
    
	<bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg index="0" ref="sqlSessionFactory"></constructor-arg>
	</bean>
</beans>