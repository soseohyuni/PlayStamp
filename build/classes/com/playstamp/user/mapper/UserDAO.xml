<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.playstamp.user.controller.IUserDAO">
				   
	<!-- 유저 입력 아이디 중복체크 -->
	<select id="userIdCheck" parameterType="String" resultType="int">
		SELECT COUNT(*) AS COUNT 
		FROM TBL_USERINFO 
		WHERE USER_ID = #{userId}
	</select>
	
	
	<!-- 회원가입 -->
	
	<!-- 1) 기본 인서트 테스트 -> 성공 
	<insert id="userInsert">
		INSERT INTO TBL_USERINFO(USER_CD, USER_ID, USER_NM, USER_TEL
		, USER_PW, USER_NICK, USER_MAIL, USER_IMG)
		VALUES ('10', #{userId}, #{userNm}, #{userTel}, #{userPw}, #{userNick}, #{userMail}, 'URL')
	</insert> -->
	
	<!-- 2) 프로시저 테스트 -->
	<insert id="userInsertProcedure" statementType="CALLABLE">
		{
			CALL PRC_USER_INSERT(#{userId}, #{userNm}, #{userTel}, #{userPw}, 
			#{userNick}, #{userMail}, 'UURL')
		}
	</insert>
	
	
	<!-- 로그인 -->
	<!-- <select id="login" resultType="package com.playstamp.use.User">
		SELECT USER_ID, USER_NM, USER_TEL, USER_NICK, USER_MAIL
		FROM TBL_USERINFO 
		where USER_ID = #{userId} AND USER_PW = #{userPw}
	</select>  -->

</mapper>